# ───────── Secrets / env ─────────
.env
.env.*
*.local
!.env.example

# ───────── Python (backend) ─────────
# Agno session store (users/sessions/runs). Local, per-machine — don't commit.
# (Also matched by *.db below, but named here so it's obvious.)
grandmacare.db
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ───────── Cognee local data (on-disk stores) ─────────
.cognee/
.cognee_home/
.artifacts/
cognee_system/
cognee_data/
.cognee_system/
.data_storage/
databases/
*.db
*.sqlite
*.sqlite3
*.lance/
.lancedb/
*.kuzu/
.artifacts/

# ───────── Node / Expo (mobile/ also has its own .gitignore) ─────────
node_modules/
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/.kotlin/
mobile/android/
mobile/ios/
*.tsbuildinfo

# ───────── Build / misc ─────────
dist/
build/
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
